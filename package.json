{
  "name": "my-flue-worker-2",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "dev": "flue dev --target cloudflare",
    "build": "flue build --target cloudflare",
    "deploy": "wrangler deploy --config dist/hello-worker/wrangler.json"
  },
  "dependencies": {
    "@flue/runtime": "^1.0.0-beta.9",
    "agents": "^0.14.2",
    "valibot": "^1.4.2"
  },
  "devDependencies": {
    "@flue/cli": "^1.0.0-beta.9",
    "wrangler": "^4.0.0"
  }
}
